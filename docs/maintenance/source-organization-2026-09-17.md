# 源码分类整理记录

日期：2026-09-17。用户同意按功能移动源码，仅调整导入及资源路径，不改变业务逻辑，并检查原浏览器入口能正常启动。

## 完成的调整

- 原根目录 32 个 Python 实现模块归入 `gameplan/` 下 8 个功能目录：`web`、`monitoring`、`ai`、`tactics`、`vision`、`skills`、`knowledge`、`core`。
- 根目录 `app.py`、`monitor_app.py` 改为轻量兼容入口，继续支持原 `app:app` 和 `monitor_app:app` 启动命令。
- 三份核心知识库 JSON 移到 `resources/knowledge/`，读取和更新路径同步调整；文件内容不变。
- 同步维护工具、测试和现用诊断脚本的导入、测试替换目标及源码读取路径。历史源码备份保持原样。
- 两份根目录旧日志在确认可独占打开后归档到 `work/logs/legacy/`；当前监控日志仍写入 `work/monitor-logs/`。
- 更新 README 和分类导航中的文件引用，增加源码及知识资源目录说明。

根目录现保留环境配置、依赖清单、使用说明、两个兼容入口与两个启动脚本。

## 启动与使用

原地址仍为 `http://127.0.0.1:8767/monitor?monitor=off`。原启动脚本内容未改动。

先验证新包下独立监控和主工作台可以在临时端口启动，再将旧根目录实现文件移入备份，保留兼容入口，重启原 8767 独立监控服务以加载新目录。重启会清空旧进程中的临时对局状态，浏览器页面可刷新后继续使用。

## 验证与已存在的问题

- 32 个业务模块经语法树对比，排除导入和本次路径调整后，业务实现一致。
- 三份核心知识 JSON 与全部原前端资源的字节校验一致。
- 当前源码、测试和诊断脚本未发现残留的旧顶层模块导入。
- 迁移前 Python：282 项通过，1 项失败，31 项子测试通过。失败为 `test_gaojianli_replay.py` 中高渐离范围特效真实截图断言；首次迁移后复测结果相同。
- 前端测试共 110 项：102 项通过、8 项失败。其中图片测试 7 项报“请选择可用的屏幕”，旧整屏测试 1 项为采样间隔预期差异。用整理前备份在隔离目录复测这两组，得到相同的 8 项失败；相关前端资源和这两份测试文件均未改动。
- 移出旧文件并切换服务后的最终 Python 回归仍为 282 项通过、同一项失败、31 项子测试通过，无新增失败。
- 原 `monitor_app:app` 经原启动脚本成功启动 8767；原 `app:app` 兼容入口在隔离数据目录与临时端口下成功启动，生成的数据库文件名仍为 `coach.sqlite3`。
- 浏览器显示工作台就绪，原监控页、屏幕列表、静态资源均正常；知识库接口读取到 132 个英雄、537 条技能。未启动实际屏幕采集。
- 检查 74 处本地文档链接，无失效目标。

本轮不修改上述既有业务或测试预期，未宣称全部测试通过，也未重新验证真实对局识别准确率。

## 备份与恢复

备份目录：`work/organization-backups/2026-09-17-source/`。

- `before.zip`：迁移前源码、调用脚本、测试和文档，共 197 个文件。
- `original-root-files/`：原根目录 32 个实现文件和三份原始知识 JSON。
- `manifest.json`：旧路径到新路径映射、调整文件清单、迁移前校验值。
- `log-moves.json`：日志归档路径与校验值。
- `integrity-check.json`：业务语法树与资源内容校验。
- `fresh-start-checks.json`：两个新包入口的独立启动检查。
- `pytest-final.xml`：最终 Python 回归结果。
- `final-startup-and-links.json`：最终启动、页面、屏幕列表与文档链接检查。
- `changes.diff`：迁移前后文本差异，便于复核导入与路径调整。
- `baseline-picture_ui.cjs.log`、`baseline-screen_monitor_ui.cjs.log`：整理前前端失败复测记录。

恢复时应先核对是否存在后续修改，停下当前服务，再按清单恢复根目录原文件与受影响的脚本、测试、文档；新目录可另行归档。不要将备份直接覆盖到有后续修改的文件上。最后用原启动脚本启动并验证监控入口。
