# 第一批分类整理记录

日期：2026-09-17。

用户要求逐步分类整理，不修改代码，并确保现用浏览器入口可以正常启动。

## 文件移动清单

| 原位置（相对项目根目录） | 新位置（相对项目根目录） |
|---|---|
| `PROJECT_HANDOFF_2026-09-12.md` | `docs/archive/2026-09-12/PROJECT_HANDOFF_2026-09-12.md` |
| `PROJECT_HANDOFF_2026-09-12_UPDATED.md` | `docs/archive/2026-09-12/PROJECT_HANDOFF_2026-09-12_UPDATED.md` |
| `GAMEPLAN_BUG_FIX_GUIDE_2026-09-12.md` | `docs/archive/2026-09-12/GAMEPLAN_BUG_FIX_GUIDE_2026-09-12.md` |
| `电竞室AI助教_完整需求与实施方案.md` | `docs/requirements/电竞室AI助教_完整需求与实施方案.md` |
| `docs/电竞室_AI_助教项目_无摄像头交付计划.md` | `docs/requirements/电竞室_AI_助教项目_无摄像头交付计划.md` |
| `docs/Qwen3-VL-8B_屏幕助教方案_评审修订稿.md` | `docs/requirements/Qwen3-VL-8B_屏幕助教方案_评审修订稿.md` |
| `docs/敌方技能释放记录与冷却提醒_需求规格.md` | `docs/requirements/敌方技能释放记录与冷却提醒_需求规格.md` |

## 文档调整

- 更新 README 的需求文档链接，增加分类导航入口。
- 更新两份历史交接和完整需求文档中的迁移路径。
- 新增 `docs/README.md`，按功能列出源码、数据、依赖和开发记录。
- 新增本整理记录；历史文档只更新文件路径，不修改业务结论。

## 保留原位

源码、前端资源、测试、启动脚本、配置、知识数据、数据库、依赖、日志和旧代码备份均保留原位。维护脚本生成的知识库索引与审计报告也保留原路径。

运行日志可能仍在写入，诊断脚本与验证资料可能被其他脚本或文档引用，因此本轮只通过导航分类，不搬动这些内容。

## 备份与恢复

原始文档及移动清单备份在 `work/organization-backups/2026-09-17/`。其中 `original-documents.zip` 保存整理前的 17 份文档，`manifest.json` 记录原位置、新位置和受保护文件的 SHA-256 校验值。

如需恢复，先核对文件没有后续编辑，再按移动清单反向移动这 7 份文档，并从备份还原有路径更新的文档。不要直接覆盖后续修改。新增导航和本记录可保留，或在恢复前更新其中的链接。

## 验证

整理前：现用 `8767` 服务和监控页面可访问；浏览器显示工作台就绪，未开始采集。

整理后检查结果：

- 179 个源码、前端资源、脚本及关键配置文件的 SHA-256 与整理前完全一致。
- 检查 72 处本地 Markdown 链接，无失效目标；17 份原始文档的备份校验通过。
- `start-monitor.ps1 -NoWindow -NoAutoStart` 正常返回原监控入口，复用已有服务。
- 原服务的健康接口、状态接口、工作台、嵌入式监控、图片及选人页面均返回 HTTP 200；页面引用的 13 项资源请求全部通过。工作台 HTML 和状态响应的校验值与整理前一致。
- 使用原 Python 环境、原配置和 `monitor_app:app` 在临时本机端口 60631 启动新进程，健康接口、状态接口、工作台、嵌入式监控及工作台脚本均返回 HTTP 200。验证后已停止该测试进程，原 `8767` 服务未重启。
- 本轮验证范围是文件完整性、启动和页面可访问性，未触发屏幕采集或模型识别，不构成对游戏识别效果的重新验收。

原始校验和启动检查结果位于备份目录的 `verification.json`、`startup-check.json` 和 `startup-check.log`。
